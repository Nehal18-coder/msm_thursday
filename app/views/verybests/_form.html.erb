<% was_validated = verybest.errors.any? %>

<%= form_with(model: verybest, local: true, novalidate: true, class: "mb-3") do |f| %>
  <%= f.button class: "btn btn-outline-secondary btn-block" %>
<% end %>
